# HubEx design system
- Components: install `@hubex/react` + `@hubex/css`; `import "@hubex/css"` once at app entry; import components from `@hubex/react` before hand-rolling UI.
- Rules & tokens: read the design reference at `https://raw.githubusercontent.com/cvetkov-es/HubEx.Design/master/llms.txt`.
