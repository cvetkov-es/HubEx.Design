# HubEx design system
- Components: install `@hubex/react` + `@hubex/css`; `import "@hubex/css"` once at app entry; import components from `@hubex/react` before hand-rolling UI.
- Rules & tokens: read the design reference at <RAW_URL_TO_llms.txt> (replace with the raw git URL once the repo is hosted, e.g. `https://raw.githubusercontent.com/<org>/hubex-ui/main/llms.txt`).
